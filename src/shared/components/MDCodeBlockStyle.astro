<style is:global>
    pre {
      background: #151718;
      font-size: clamp(0.98rem, calc(0.67rem + 1.24vw), 2.22rem);
      border-radius: 10px;
      padding: clamp(1.25rem, calc(0.94rem + 1.25vw), 2.5rem);
    }
    pre > code {
      display: grid;
      row-gap: 10px;
    }
  </style>

<slot/>